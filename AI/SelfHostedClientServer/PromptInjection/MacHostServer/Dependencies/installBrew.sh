#!/bin/bash

# NOTE: For best results, use an M series (ie: M1, M2, etc.) Mac
# Install Brew in the event that the host does not have a package manager
# Will need to input password and then set brew as an 
# environment variable as the instructions from the installer dictate
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
